<nav class="navbar navbar-expand-md navbar-light shadow-sm sticky-top custom-navbar">
  <div class="container-fluid px-4">
    <a title="YatraStay" class="navbar-brand fw-bold fs-3 text-danger" href="/listings">
      <i class="fa-regular fa-compass me-2"></i>YatraStay
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <!-- Left Nav -->
      <div class="navbar-nav">
        <a class="nav-link" href="/listings" title="Home">
          <i class="fa-solid fa-house me-1"></i> Home
        </a>
        <a class="nav-link" href="/listings" title="Explore Listings">
          <i class="fa-solid fa-location-dot me-1"></i> Explore
        </a>
      </div>

      <!-- Center Search -->
      <div class="mx-auto">
        <form class="d-flex" role="search">
          <input class="form-control search-inp" type="search" placeholder="Search destinations" />
          <button class="btn search-btn ms-2" type="submit">
            <i class="fa-solid fa-magnifying-glass"></i> Search
          </button>
        </form>
      </div>

      <!-- Right Nav -->
      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/listings/new" title="Add Listing">
          <i class="fa-solid fa-plus me-1"></i> Add Listing
        </a>
        <% if(!currUser){ %>
        <a class="nav-link" href="/signup">Signup</a>
        <a class="nav-link" href="/login">Login</a>
        <% } else { %>
        <a class="nav-link" href="/logout">Logout</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>


<script>
  window.addEventListener('scroll', () => {
    const navbar = document.querySelector('.custom-navbar');
    if (window.scrollY > 10) {
      navbar.classList.add('scrolled');
    } else {
      navbar.classList.remove('scrolled');
    }
  });
</script>
